pub fn main() !void {
    // Prints to stderr, ignoring potential errors.
    std.debug.print("Integration test has not yet been setup\n", .{});
}

const std = @import("std");
const seq_zig = @import("seq_zig");
